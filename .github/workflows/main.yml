name: Run Crypto Bot

on:
  schedule:
    - cron: '0 * * * *'  # တစ်နာရီတစ်ခါ ပုံမှန် Run မည်
  workflow_dispatch:      # ကိုယ့်ဘာသာ Button နှိပ်ပြီး စမ်းသပ်နိုင်ရန်

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # google-generativeai library ပါဝင်စေရန် requirements.txt ကို စစ်ဆေးပါ
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Python Script
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}  # AI Key အတွက် ဒါကို ထည့်သွင်းထားပါတယ်
        run: python crypto_news_bot.py
